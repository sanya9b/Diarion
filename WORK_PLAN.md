# План виконання робіт (Покращення Diarion)

## Етап 1: Оптимізація та нормалізація дат у базі даних (Date Normalization)
- [x] Перевірка та приведення всіх дат (`Date`) до `DateTime.Date` (без компоненту часу) у моделях та `DiaryService` перед збереженням в LiteDB.
- [x] Написання/оновлення Unit-тестів у `DiaryServiceTests.cs`.
- [x] Перевірка компіляції (`dotnet build`).

## Етап 2: Усунення дублювання фінансових полів (Finance Fields Cleanup)
- [x] Видалення старих полів `Expenses` та `Income` з моделі `DiaryEntry`.
- [x] Очищення XAML (якщо ці поля використовуються у `DiaryDetailPage` чи `MainPage`).
- [x] Написання/оновлення тестів.
- [x] Перевірка компіляції.

## Етап 3: Надійність автозбереження (AutoSave Reliability)
- [x] Додавання механізму `FlushAutoSaveAsync()` у `MainViewModel` для негайного збереження.
- [x] Виклик цього методу в `OnDisappearing` у `MainPage.xaml.cs` або при зміні вкладок.
- [x] Додавання Unit-тестів для перевірки переривання/виконання автозбереження.
- [x] Перевірка компіляції.

## Етап 4: Плавна зміна теми та мови (Без перезавантаження сторінки)
- [x] Видалення "жорсткого" перезавантаження `window.Page = new AppShell();` з `AppShell.xaml.cs`.
- [x] Налаштування коректного `AppThemeBinding` або динамічного оновлення ресурсів.
- [x] Перевірка компіляції.

## Етап 5: Оптимізація UI (Розбиття MainPage)
- [x] Розбиття перевантаженого `MainPage.xaml` на окремі компоненти (`Controls/Diary/SleepSection.xaml`, `HealthSection.xaml` тощо).
- [x] Перевірка Binding.
- [x] Перевірка компіляції.
