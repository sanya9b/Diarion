<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- allowBackup=false: keep the local diary off Google cloud / adb backup (privacy-first, no cloud). -->
	<application android:allowBackup="false" android:fullBackupContent="false" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true"></application>
	<!-- No ACCESS_NETWORK_STATE, and no INTERNET either: the release APK asks for neither, so the
	     OS itself guarantees the diary cannot leave the phone. Nothing in the app reads connectivity.
	     Adding a dependency can undo this silently — Google's ML Kit AAR, for one, declares both in
	     its own manifest and the merger injects them. Check the generated release manifest after
	     adding any package. -->
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.VIBRATE" />
	<uses-permission android:name="android.permission.USE_BIOMETRIC" />
	<uses-permission android:name="android.permission.USE_FINGERPRINT" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
</manifest>
